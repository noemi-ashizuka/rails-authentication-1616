<% if user_signed_in? %>
  <h1>Welcome <%= current_user.name %></h1>
<% else %>
  <h1>Welcome! Please log in</h1>
<% end %>
  
<p>Devise is a cool gem</p>
<p>Let's use git branches</p>

<%#= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete } %>
